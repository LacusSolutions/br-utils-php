{
  "name": "lacus-br-utils-monorepo",
  "description": "Configuração do monorepo para utilitários brasileiros",
  "version": "1.0.0",
  "packages": [
    "packages/*"
  ],
  "scripts": {
    "build": "composer install --no-dev --optimize-autoloader",
    "test": "composer test",
    "test-all": "composer test && composer analyse && composer check",
    "release": "composer build && git tag -a v1.0.0 -m 'Release v1.0.0' && git push origin v1.0.0"
  },
  "config": {
    "sort-packages": true,
    "allow-plugins": {
      "infection/extension-installer": true
    }
  },
  "extra": {
    "monorepo": true,
    "packages": [
      "packages/cpf-fmt",
      "packages/cpf-gen",
      "packages/cpf-val",
      "packages/cpf-utils",
      "packages/cnpj-fmt",
      "packages/cnpj-gen",
      "packages/cnpj-val",
      "packages/cnpj-utils",
      "packages/br-utils"
    ]
  }
}
